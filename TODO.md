# Features

# Fixes

# Testing
- Add unit tests for the orchestrator (internal/application/tickets/orchestrator.go) — the most critical untested path: ticket lifecycle, worktree creation, provider execution, state transitions

